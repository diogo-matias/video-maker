import { _Input } from '../classes';
import { Paginated } from '../clustering';
import { ApiClient, ApiReqParams } from './client';
export default class ClusteringApiClient extends ApiClient {
    rootPath: string;
    get(path: string, params?: ApiReqParams): Promise<any>;
    getPaginated<T>(path: string, field: string, mapFn: (raw: any) => T, limit?: number, params?: ApiReqParams): Paginated<T>;
    postItems(path: string, items: _Input<string>[], params?: ApiReqParams & {
        forceNewClusters?: boolean;
        forceClusterId?: number;
    }): Promise<any>;
}
