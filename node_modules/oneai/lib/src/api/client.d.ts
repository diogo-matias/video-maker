import Logger from '../logging';
export declare const uuid: string;
export declare const agent: string;
export interface ApiClientParams {
    apiKey: string;
    baseURL: string;
    timeout: number;
}
export declare type ApiReqParams = Partial<ApiClientParams>;
export declare class ApiClient {
    private agent;
    params: ApiClientParams;
    logger: Logger;
    constructor(params: ApiClientParams, logger: Logger);
    get(path: string, params?: ApiReqParams): Promise<any>;
    post(path: string, data: any, params?: ApiReqParams): Promise<any>;
    validateApiKey(params?: ApiReqParams): string;
}
