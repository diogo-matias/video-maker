export default class Logger {
    enabled: boolean;
    prefix: string;
    constructor(enabled?: boolean);
    debug(message: string): void;
    debugNoNewline(message: string): void;
    error(message: string): void;
}
export declare function timeFormat(time: number): string;
